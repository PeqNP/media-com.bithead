<% template/header %>

  <h1>Create your @ys account</h1>

  <p>@ys is currently in closed beta. To join the beta program, please provide the following information:

  <script>
  var error = "{error}";
  if error.length > 0 {
    var elem = document.getElementById("error-message");
    elem.style.display = "block";
  }
  </script>
  <div id="error-message">{error}</div>

  <form action="/account/create" method="post">
    <label for="fullname">Full name</label>
    <input type="text" name="fullname" value="{fullname}">

    <label for="orgpath">Company domain</label>
    <input type="text" name="orgpath" value="{orgpath}">
    <div class="empty"></div>
    <div class="subtitle">e.g. tesla.com, x.com, starlink.com</div>

    <label for="email">Email</label>
    <input type="text" name="email" value="{email}">

    <label for="password">Password</label>
    <input type="password" name="password" value="{password}">

    <div class="empty"></div>
    <input type="submit" value="Sign up">
  </form>

<% template/footer %>
